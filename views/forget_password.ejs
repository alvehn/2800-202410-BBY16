<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Reset Password</title>
    <link rel="icon" href="/assets/favicon.png" type="image/x-icon">
</head>
<body class="flex items-center justify-center h-screen bg-gray-100 bg-cover bg-center" style="background-image: url('background3.webp');">

    <div class="w-full max-w-md p-8 bg-white shadow-md rounded-md">
        <h1 class="text-2xl font-semibold mb-6 text-center">Reset your password via email</h1>
        <% if (userNotFound) { %>
            <p class="text-red-500 mb-4 text-center">Invalid email</p>
        <% } %>
        <form action="/reset_password" method="post" class="space-y-6">
            <div>
                <input id="email" name="email" type="email" placeholder="Email" class="w-full p-3 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500" required>
            </div>
            <div class="flex justify-between items-center">
                <button disabled id="nextButton" type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 disabled:opacity-50">Next</button>
                <a href="/" class="text-blue-500 hover:underline">Cancel</a>
            </div>
        </form>
    </div>
    <script>
        const emailInput = document.getElementById('email');
        const nextButton = document.getElementById('nextButton');
        emailInput.addEventListener('input', () => {
            nextButton.disabled = !emailInput.value.trim();
        });
    </script>
</body>
</html>
